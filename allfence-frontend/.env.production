# Production build config. The frontend and API are served from the same
# Vercel deployment/domain, so the API is reached with a relative path -
# no cross-origin URL or CORS setup needed.
VITE_API_URL=/api
